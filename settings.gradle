pluginManagement {
    repositories {
        gradlePluginPortal()  // Where Spring Boot plugin lives
        mavenCentral()        // Optional fallback
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()        // All dependencies come from Maven Central
    }
}
rootProject.name = 'mjg-cp-mindtree-connector'